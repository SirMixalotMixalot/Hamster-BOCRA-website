TASK BRIEF — KG (Lead / System Integrator)
==================================================

PROJECT: BOCRA Website — Hackathon
ROLE: Oversee all workstreams, system integration, Customer + Admin panels, 
      backend API, AI chatbot integration, content upload, testing & QA
STACK: React + TypeScript (Frontend), FastAPI + Python (Backend), Supabase, Vercel


YOUR RESPONSIBILITIES
----------------------
You are the glue. You touch every part of the system and make sure it all works together.

1. Oversee & unblock team members
2. Review and clean code from Phemo and BK
3. Build/refine Customer and Admin panels (with Phemo)
4. Build backend API endpoints (with BK)
5. Integrate Tiro's chatbot into the main app
6. Upload and filter Ayanda & Weno's content into the system
7. End-to-end testing and QA


=================================================================
DAY 1 — FOUNDATIONS
=================================================================

MORNING: Review & Unblock
---------------------------
[ ] Pull latest, run frontend (npm run dev), verify landing page works
[ ] Run backend (uvicorn app.main:app --reload), verify /health returns ok
[ ] Check BK has Vercel deploying correctly — test the live URL
[ ] Check BK has Supabase project created with auth enabled
[ ] Check Phemo has started on CustomerLayout.tsx and routing
[ ] Check Tiro has his chatbot/ folder set up and server running on port 8001
[ ] Check Ayanda & Weno have started their Google Doc

AFTERNOON: Customer Panel Foundation (with Phemo)
---------------------------------------------------
[ ] Set up React Router routes in App.tsx:
    /customer/*  → wrapped in CustomerLayout
    /admin/*     → wrapped in AdminLayout
    Protect both with auth check (redirect to / if not logged in)

[ ] Build CustomerLayout.tsx:
    - Sidebar with nav links (Dashboard, Applications, Profile, etc.)
    - Header with user name + logout button
    - Main content area that renders child routes
    - AI Chatbot available on all pages (reuse existing component)
    - Same BOCRA color palette, no-scroll viewport fit

[ ] Build AdminLayout.tsx:
    - Sidebar: Dashboard, Applications, Customers, Content, Reports, Settings
    - Admin badge in header
    - Same layout principles as CustomerLayout

[ ] Connect SignInModal to actual Supabase Auth:
    - Wire up the login form to supabase.auth.signInWithPassword()
    - Wire up register form to supabase.auth.signUp()
    - Wire up Google OAuth to supabase.auth.signInWithOAuth()
    - On success: check profile.role → redirect to /customer/ or /admin/
    - Store session in React context or zustand

EVENING: Backend API Foundations (with BK)
--------------------------------------------
[ ] Verify Supabase tables are created (check BK's work)
[ ] Build auth middleware in FastAPI:
    - Validate Supabase JWT on every request
    - Extract user_id and role from token
    - Create dependency: get_current_user()
    - Create dependency: require_admin()

[ ] Build first API endpoints:
    GET  /api/auth/me          → return current user profile
    GET  /api/applications     → list user's applications
    POST /api/applications     → create application (draft)

[ ] Test endpoints with curl or FastAPI /docs


=================================================================
DAY 2 — BUILD & INTEGRATE
=================================================================

MORNING: Customer Portal Pages
---------------------------------
[ ] Customer Dashboard:
    - Fetch and display user's application count, active licences, notifications
    - Quick action buttons wired to correct routes
    - Use mock data where API isn't ready yet

[ ] Applications List Page:
    - Fetch from GET /api/applications
    - Table with reference number, type, date, status badge
    - Click row → navigate to /customer/applications/:id

[ ] New Application Flow:
    - Multi-step form (licence type → documents → review → payment → confirm)
    - POST to /api/applications on submit
    - Document upload via Supabase Storage
    - Stripe payment integration:
      npm install @stripe/react-stripe-js @stripe/stripe-js
      Backend: POST /api/payments/create-intent
      Frontend: Stripe Elements for card input

[ ] Profile Page:
    - Fetch from GET /api/auth/me
    - Editable fields, save via PATCH /api/profiles/:id
    - Profile photo upload to Supabase Storage

AFTERNOON: Admin Panel Pages
-------------------------------
[ ] Admin Dashboard:
    - Total applications, pending count, revenue stats
    - Charts with recharts (npm install recharts)
    - Recent application activity feed

[ ] Application Management:
    - Table of ALL applications (admin sees everything)
    - Filter by: status, type, date range
    - Click → review application detail
    - Approve/Reject buttons that REQUIRE decision_reason
    - POST to /api/applications/:id/decide
    - Status change logged to application_status_log

[ ] Customer Management:
    - Table of all registered users
    - Search by name, email, ID number
    - Click → view customer's applications and payments

EVENING: Backend API Completion
----------------------------------
[ ] Complaints API:
    POST /api/complaints         → submit complaint
    GET  /api/complaints         → list (user: own, admin: all)
    PATCH /api/complaints/:id    → admin respond/update status

[ ] Documents API:
    POST /api/documents/upload   → upload to Supabase Storage
    GET  /api/documents/:id      → signed download URL

[ ] Search API:
    GET /api/search?q=...        → full-text search across news, decisions, docs
    Wire to Header search bar in frontend

[ ] News & Stats API:
    GET  /api/news               → public news feed
    POST /api/news               → admin create
    GET  /api/stats              → telecom statistics

[ ] Audit logging middleware:
    - Log every create/update/delete/approve/reject to audit_log table
    - Include user_id, action, resource_type, resource_id, IP, timestamp


=================================================================
DAY 3 — INTEGRATION, CONTENT, TESTING
=================================================================

MORNING: Integrate Tiro's Chatbot
------------------------------------
[ ] Review Tiro's knowledge_base.json — check quality, fix bad entries
[ ] Copy chatbot/search.py → backend/app/ai/pipeline.py (adapt imports)
[ ] Copy chatbot/knowledge_base.json → backend/app/ai/knowledge_base.json
[ ] Create backend endpoint POST /api/ai/chat that uses the search logic
[ ] Update Frontend AIChatbot.tsx:
    - Replace keyword matching with fetch to POST /api/ai/chat
    - Display source links in responses (clickable)
    - Keep all existing UI/animations exactly the same
    - Add error handling (if API down, show "contact us" fallback)

MIDDAY: Upload Ayanda & Weno's Content
-----------------------------------------
[ ] Review their Google Doc for completeness and accuracy
[ ] Upload news articles to Supabase news table:
    - Take their G1 (Recent News) section
    - Insert each article with title, description, tag, published=true

[ ] Upload telecom statistics to Supabase telecom_stats table:
    - Take their F1 (Statistics) data
    - Insert metrics with values, units, periods

[ ] Upload regulatory decisions to Supabase regulatory_decisions table:
    - Take their E1 (Legislation) and E2 (Regulations) data

[ ] Upload FAQ entries to Tiro's knowledge_base.json:
    - Take their Section I (FAQs)
    - Convert to knowledge_base.json format with keywords
    - Merge with Tiro's existing entries, remove duplicates

[ ] Upload documents to Supabase Storage public-documents bucket:
    - Take their H1 (Forms) download links
    - If PDFs are available, upload them
    - If not, create placeholder entries in the documents table

[ ] Upload About BOCRA content:
    - Leadership names/bios → can be hardcoded in frontend for now
    - Mandate, vision, mission → About page content

AFTERNOON: Testing & QA
--------------------------

FUNCTIONAL TESTING — go through every user flow:
[ ] Landing page:
    - All 4 mega-menu sections open, items display correctly
    - Search bar opens, can type (results show if search API connected)
    - "Apply for License" → SignInModal → customer-choice step
    - "Sign In" → SignInModal → role selection
    - AI chatbot opens, responds to questions, minimize/close works
    - Explore Services overlay opens/closes
    - News overlay opens/closes
    - Bottom bar links work (phone, email, social icons)
    - Contact Modal: call, email, AI assistant buttons work
    - Mobile responsive — test at 375px, 768px, 1024px widths

[ ] Authentication:
    - Register new customer account → email confirmation
    - Login with email/password → redirects to /customer/dashboard
    - Login as admin → redirects to /admin/dashboard
    - Google OAuth login works
    - Logout clears session → back to landing page
    - Protected routes redirect to / when not logged in
    - Wrong password shows error
    - Forgot password sends reset email

[ ] Customer Portal:
    - Dashboard shows correct stats
    - Profile: view, edit, save, photo upload
    - New Application: all steps work, form validation, document upload
    - Payment: Stripe form loads, test payment succeeds
    - My Applications: list loads, click shows detail, status shows
    - Licence Verification: enter number, get result
    - Support: submit ticket, view ticket history
    - Documents: list loads, can download/view

[ ] Admin Panel:
    - Dashboard stats load
    - Applications table: filter, search, sort work
    - Review application: approve (with reason), reject (with reason)
    - Status change reflected in customer's view
    - Customer list: search, click to view detail
    - News: create, edit, publish an article
    - Statistics: add/update a stat entry

[ ] AI Chatbot (across all pages):
    - Responds correctly to: licence, complaint, contact, type approval
    - Shows source links that are clickable
    - Fallback response when no match
    - Works on landing page AND authenticated pages

[ ] Cross-cutting:
    - No console errors in browser DevTools
    - No broken images or missing assets
    - All forms validate before submit (no empty required fields)
    - Loading states show while data fetches
    - Error states show when API fails
    - Responsive on mobile, tablet, desktop

SECURITY TESTING:
[ ] Can a customer access /admin/* routes? (should be blocked)
[ ] Can a customer see another customer's applications? (should be blocked)
[ ] Do API endpoints reject requests without valid JWT?
[ ] Do admin endpoints reject requests from customer role?
[ ] File uploads: can user upload .exe or .js? (should be blocked — PDF/JPG/PNG only)
[ ] SQL injection: try submitting ' OR 1=1 in form fields (should be sanitized)
[ ] XSS: try submitting <script>alert(1)</script> in text fields (should be escaped)

BUG TRACKING:
- Keep a running list of bugs found
- Priority: P1 (blocks something), P2 (looks bad), P3 (nice to fix)
- Fix P1s immediately, P2s before submission, P3s if time allows


=================================================================
TEAM COORDINATION CHECKLIST
=================================================================

Daily standups (even 5 min):
[ ] What did you finish?
[ ] What are you working on?
[ ] Are you blocked on anything?

Integration points to coordinate:
  Phemo ↔ KG:  Frontend page components, routing, layout
  BK ↔ KG:     API response shapes, Supabase schema changes, deployment
  Tiro ↔ KG:   Knowledge base quality, integration into main backend
  Ayanda/Weno ↔ KG:  Content review, data upload format

Pull request discipline:
  - Everyone works on their own branch
  - PR into main requires at least a quick review
  - BK should have Vercel preview deploys on PRs
  - Don't merge broken code — test locally first


=================================================================
YOUR DAILY SCHEDULE
=================================================================

DAY 1:
  AM  — Review everyone's setup, unblock issues, build layouts + routing
  PM  — CustomerLayout, AdminLayout, auth flow with Supabase
  EVE — Backend auth middleware, first API endpoints

DAY 2:
  AM  — Customer portal pages (dashboard, applications, profile)
  PM  — Admin panel pages, Stripe payment flow
  EVE — Remaining API endpoints, search, audit logging

DAY 3:
  AM  — Integrate Tiro's chatbot, upload Ayanda/Weno's content
  PM  — Full testing pass, bug fixing
  EVE — Final polish, deployment check, prepare demo
